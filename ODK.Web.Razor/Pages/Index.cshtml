@page "/"
@using ODK.Core.Platforms
@model IndexModel
@{
    Layout = "_HomeLayout";
    Model.Path = "/";
}

@if (Model.Platform == PlatformType.DrunkenKnitwits)
{
    @await Html.PartialAsync("Home/_OdkHomeContent", Model.ComponentContext)
}
else
{
    
}