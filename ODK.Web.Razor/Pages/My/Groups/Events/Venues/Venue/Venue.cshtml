@page "/my/groups/{slug}/events/venues/{chapterVenueId:guid}"
@using ODK.Services.Venues
@using ODK.Web.Razor.Models.Components
@model VenueModel
@inject IVenueAdminService VenueAdminService
@{
    Layout = "_GroupAdminLayout";

    var request = Model.MemberChapterAdminServiceRequest;
    var viewModel = await VenueAdminService.GetVenueViewModel(request, Model.ChapterVenueId);
}

@await Html.PartialAsync("Components/_TwoColLeftMenu", new TwoColLeftMenuViewModel
{
    Title = viewModel.ChapterVenue.GetName(),
    Breadcrumbs = new BreadcrumbsViewModel
    {
        Breadcrumbs = new[]
        {
            new MenuItem
            {
                Link = Model.OdkRoutes.GroupAdmin.Venues(Model.Chapter).Path,
                Text = "Back to venues",
                Icon = IconType.ArrowLeft
            }
        }
    },
    BodyContentFunc = @<div>@await Html.PartialAsync("Admin/Venues/_VenueContent", viewModel)</div>,
    MenuTitle = "Admin",
    MenuContentFunc = _ => Html.PartialAsync("Admin/_AdminSideMenu")
})

@section scripts
{
    @await Html.PartialAsync("Components/Scripts/_GoogleLocation")
}