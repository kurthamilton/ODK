@page "/my/groups/{id:guid}/events/{eventId:guid}/responses"
@using ODK.Services.Events
@model ResponsesModel
@inject IEventAdminService EventAdminService
@{
    Layout = "_GroupAdminLayout";
    Model.Title = "Event responses";

    var viewModel = await EventAdminService.GetEventAttendeesViewModel(Model.AdminServiceRequest, Model.EventId);
}

@await Html.PartialAsync("Admin/Events/_EventResponsesContent", viewModel)