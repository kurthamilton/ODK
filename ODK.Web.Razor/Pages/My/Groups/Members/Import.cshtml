@page "/my/groups/{slug}/members/import"
@using ODK.Services.Members
@using ODK.Web.Razor.Models.Components
@model ImportModel
@inject IMemberImportService MemberImportService
@{
    Layout = "_GroupAdminLayout";
    Model.Title = "Import members";

    var viewModel = await MemberImportService.GetMemberImportViewModel(
        Model.MemberChapterAdminServiceRequest);
}

@await Html.PartialAsync("Components/_TwoColLeftMenu", new TwoColLeftMenuViewModel
{
    Title = "Import members",
    Breadcrumbs = new BreadcrumbsViewModel
    {
        Breadcrumbs = new[]
        {
            new MenuItem
            {
                Link = Model.OdkRoutes.GroupAdmin.Members(Model.Chapter).Path,
                Text = "Back to members",
                Icon = IconType.ArrowLeft
            }
        }
    },
    BodyContentFunc = @<div>@await Html.PartialAsync("Admin/Members/_ImportMembersContent", viewModel)</div>,
    MenuTitle = "Admin",
    MenuContentFunc = _ => Html.PartialAsync("Admin/_AdminSideMenu")
})
