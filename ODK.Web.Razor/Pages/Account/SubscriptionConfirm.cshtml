@page "/account/subscription/confirm"
@attribute [Authorize]
@using Microsoft.AspNetCore.Authorization
@using ODK.Core.Exceptions
@using ODK.Core.Payments
@using ODK.Core.Platforms
@using ODK.Web.Common.Routes
@using ODK.Web.Razor.Models.Account
@using ODK.Web.Razor.Models.Components
@using ODK.Web.Razor.Models.Payments
@model SubscriptionConfirmModel
@inject IOdkRoutes OdkRoutes
@{
    Layout = "_AccountLayout";
    Model.Title = "Subscription confirm";

    if (Model.Platform == PlatformType.DrunkenKnitwits)
    {
        throw new OdkNotFoundException();
    }
}

@await Html.PartialAsync("Components/_Body", new BodyViewModel
{
    Title = "Account",
    HideSubscriptionAlert = true,
    Content = await Html.PartialAsync("Account/_SiteAccountLayout", new SiteAccountLayoutViewModel
    {
        Active = Model.OdkRoutes.Account.Subscription(null),
        Title = "",
        Content =
            @<div>
                @await Html.PartialAsync("Components/_Breadcrumbs", new BreadcrumbsViewModel
                {
                    Breadcrumbs = new[]
                    {
                        new MenuItem { Text = "My subscription", Link = OdkRoutes.Account.Subscription(chapter: null) },
                        new MenuItem { Text = "Checkout" }
                    }
                })

                <section class="section">
                    @await Html.PartialAsync("Payments/_PaymentConfirmContent", new PaymentConfirmViewModel
                    {
                        Chapter = null,
                        SessionId = Model.SessionId
                    })
                </section>
            </div>
    })
})

@section scripts 
{
    @await Html.PartialAsync("Payments/_PaymentConfirmScripts")
}