@page "/siteadmin/geolocation"
@attribute [Authorize]
@using Microsoft.AspNetCore.Authorization
@model GeolocationModel
@{
    Layout = "_SiteAdminLayout";
    Model.Title = "Geolocation | Site Admin";
}

@await Html.PartialAsync("SiteAdmin/_GeolocationContent")

<hr class="my-4" />

@await Html.PartialAsync("SiteAdmin/_DirectoryBrowserContent", Model.BrowsePath)
