@page "/siteadmin/referrals"
@attribute [Authorize]
@using Microsoft.AspNetCore.Authorization
@model ReferralsModel
@{
    Layout = "_SiteAdminLayout";
    Model.Title = "Referrals | Site Admin";
}

@await Html.PartialAsync("SiteAdmin/_ReferralCampaignsContent")
