@page "/{chapterName}/admin/siteadmin/theme"
@using ODK.Services.Chapters.ViewModels
@using ODK.Web.Razor.Models.Admin
@model ThemeModel
@{
    Layout = "_OdkChapterAdminLayout";
    Model.Title = "Theme | Site Admin";

    // Site admin page, not a group-owner one, so the subscription feature never gates it.
    var viewModel = new ChapterThemeAdminPageViewModel
    {
        Chapter = Model.Chapter,
        CanEdit = true
    };
}

@await Html.PartialAsync("Admin/_AdminBody", new AdminBodyViewModel
{
    Title = "Theme",
    ContentFunc = @<div>@await Html.PartialAsync("Admin/Chapter/_ThemeContent", viewModel)</div>
})