@page "/{chapterName}/Admin"
@using ODK.Web.Common.Routes
@model IndexModel
@{
    Layout = "_OdkChapterAdminLayout";

    var chapter = await Model.GetChapter();
    Response.Redirect(OdkRoutes.MemberGroups.GroupTexts(Model.Platform, chapter));
}
