@page "/{chapterName}/Admin/Members/Admins/{id:guid}"
@using ODK.Services.Members
@using ODK.Web.Common.Routes
@using ODK.Web.Razor.Models.Admin
@using ODK.Web.Razor.Models.Components
@using ODK.Web.Common.Components
@model AdminMemberModel
@inject IMemberAdminService MemberAdminService
@{
    Layout = "_OdkChapterAdminLayout";
    Model.Title = "Edit admin member | Admin";

    var request = Model.MemberChapterAdminServiceRequest;
    var viewModel = await MemberAdminService.GetAdminMemberViewModel(request, Model.MemberId);
}

@await Html.PartialAsync("Admin/_AdminBody", new AdminBodyViewModel
{
    Title = viewModel.AdminMember.Member.FullName,
    Breadcrumbs = new[]
    {
        new MenuItem 
        { 
            Icon = IconType.ArrowLeft,
            Link = Model.AdminRoutes.AdminMembers(Model.Chapter).Path, 
            Text = "Back to admins" 
        }
    },
    ContentFunc = @<div>@await Html.PartialAsync("Admin/Members/_AdminMemberContent", viewModel)</div>
})