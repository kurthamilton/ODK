@using ODK.Web.Common.Routes
@model ODK.Web.Razor.Models.Members.MemberAvatarViewModel
@{
    var imageUrl = OdkRoutes.Members.Avatar(Model.Member.Id, Model.ChapterName); ;
    var maxWidth = Model.MaxWidth > 0 ? Model.MaxWidth : 75;
}

<img style="@(maxWidth > 0 ? $"max-width: {maxWidth}px" : null)"
     class="@(Model.IsTop ? "card-img-top" : "card-img")"
     src="@imageUrl"
     alt="Photo of @Model.Member.FullName"
     data-img-fallback="/assets/img/member.png"
     onerror="setImageError(this)" />
