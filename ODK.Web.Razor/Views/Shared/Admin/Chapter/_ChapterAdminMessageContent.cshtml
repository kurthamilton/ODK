@using ODK.Core.Utils
@using ODK.Services.Chapters.ViewModels
@using ODK.Web.Razor.Models.Admin.Chapters
@using ODK.Web.Razor.Models.Components
@using ODK.Web.Razor.Mvc
@model ChapterMessageAdminPageViewModel
@await Html.PartialAsync("Components/_AdminPageDescription",
    "A message sent through your contact form, and your reply.")

@{
    var created = Model.Message.CreatedUtc.ToFriendlyDateTimeString(new FriendlyDateStringOptions
    {
        IncludeDayOfWeek = true,
        TimeZone = Html.DisplayTimeZone(Model.Chapter.TimeZone)
    });
}

<div class="section-stack">
    <div>
        <div class="form-group mb-3">
            <label class="form-label" for="from">From</label>
            <input type="text" readonly class="form-control-plaintext" id="from" value="@Model.Message.FromAddress">
        </div>

        <div class="form-group mb-3">
            <label class="form-label" for="sent">Sent</label>
            <input type="text" readonly class="form-control-plaintext" id="sent"
                value="@created">
        </div>

        <div class="form-group mb-3">
            <label class="form-label">Original message</label>
            <p>
                @Model.Message.Message
            </p>
        </div>

        @if (Model.Message.RepliedUtc != null && Model.Replies.Count == 0)
        {
            <div class="form-group mb-3">
                <label class="form-label">Replied</label>
                <p>
                    @Model.Message.RepliedUtc.Value.ToFriendlyDateTimeString(new FriendlyDateStringOptions { IncludeDayOfWeek = true, TimeZone = Html.DisplayTimeZone(Model.Chapter.TimeZone) })
                </p>
            </div>
        }
    </div>

    @if (Model.Replies.Count > 0)
    {
        @await Html.PartialAsync("Components/_Section", new SectionViewModel
        {
            Heading = new HeadingViewModel
            {
                Title = "Replies",
                Type = HeadingType.H3
            },
            BodyContentFunc =
                @<div class="table-responsive">
                    <table class="table table-striped">
                        <thead>
                            <tr>
                                <th>Sent</th>
                                <th>From</th>
                                <th>Message</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var reply in Model.Replies.OrderBy(x => x.CreatedUtc))
                            {
                                <tr>
                                    <td>@reply.CreatedUtc.ToFriendlyDateTimeString(new FriendlyDateStringOptions { IncludeDayOfWeek = true, TimeZone = Html.DisplayTimeZone(Model.Chapter.TimeZone) })</td>
                                    <td>@reply.Member?.FullName</td>
                                    <td>@Html.Raw(reply.Message)</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
        })
    }

    @await Html.PartialAsync("Components/_Section", new SectionViewModel
    {
        Heading = new HeadingViewModel
        {
            Title = "Reply",
            Type = HeadingType.H3
        },
        BodyContentFunc =
            @<div>
                @if (Model.Message.RepliedUtc == null)
                {
                    <div class="mb-3">
                        <button type="button" class="btn btn-link px-0"
                            data-bs-toggle="collapse" data-bs-target="#mark-replied">
                            Already replied?
                        </button>
                        <div class="collapse" id="mark-replied">
                            <form action="/groups/@Model.Chapter.Id/messages/@Model.Message.Id/replied" method="post">
                                @Html.AntiForgeryToken()
                                <button class="btn btn-primary">Set message as replied</button>
                            </form>
                        </div>
                    </div>
                }

                <form action="/groups/@Model.Chapter.Id/messages/@Model.Message.Id/reply" method="post">
                    @Html.AntiForgeryToken()
                    @await Html.PartialAsync("Admin/Chapter/_ChapterAdminMessageForm", new ChapterMessageReplyFormViewModel
                    {
                        Message = string.Empty
                    })
                    <button class="btn btn-primary">Send</button>
                </form>
            </div>
    })
</div>
