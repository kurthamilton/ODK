@using ODK.Services.Chapters.ViewModels
@using ODK.Web.Razor.Models.Admin.Chapters
@model ChapterTextsAdminPageViewModel

@await Html.PartialAsync("Components/_AdminPageDescription",
    "Wording specific to your group: its description, and the messages new members see.")

<form method="post" action="/groups/@Model.Chapter.Id/texts">
    @Html.AntiForgeryToken()

    @await Html.PartialAsync("Admin/Chapter/_TextForm", new ChapterTextsFormViewModel
    {
        DescriptionHtml = Model.Texts?.DescriptionHtml,
        RegisterMessageHtml = Model.Texts?.RegisterTextHtml,
        ShortDescription = Model.Texts?.ShortDescription,
        ValidateUrl = $"/groups/{Model.Chapter.Id}/texts/validate",
        WelcomeMessageHtml = Model.Texts?.WelcomeTextHtml
    })
    <button class="btn btn-primary">Update</button>
</form>
