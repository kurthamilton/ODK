@using ODK.Core.Chapters
@using ODK.Core.Platforms
@using ODK.Web.Common.Components
@using ODK.Web.Common.Extensions
@using ODK.Web.Common.Routes
@using ODK.Web.Razor.Models
@using ODK.Web.Razor.Models.Admin.Venues
@using ODK.Web.Razor.Models.Components
@model OdkComponentViewModel<Chapter>

@await Html.PartialAsync("Components/_Breadcrumbs", new BreadcrumbsViewModel
{
    Breadcrumbs = new[]
    {
        new MenuItem
        {
            Link = OdkRoutes.MemberGroups.Venues(Model.Platform, Model.Value),
            Text = "Venues"
        },
        new MenuItem { Text = "New" }
    }
})

<h2>Create venue</h2>

<section class="section">
    <form method="post">
        @*@Html.AntiForgeryToken()*@

        @await Html.PartialAsync("Admin/Venues/_VenueForm", new VenueFormViewModel())

        <button type="submit" class="btn btn-primary">Create</button>
    </form>
</section>