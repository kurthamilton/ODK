@using ODK.Core.Chapters
@using ODK.Services
@using ODK.Services.Chapters
@using ODK.Services.Chapters.ViewModels
@using ODK.Web.Common.Services
@using ODK.Web.Razor.Models
@using ODK.Web.Razor.Models.Components
@model OdkComponentViewModel<Chapter>
@inject IChapterAdminService ChapterAdminService
@{
    var serviceRequest = Model.MemberChapterServiceRequest(Model.Value.Id);
    var viewModel = await ChapterAdminService.GetSuperAdminChapterEmailsViewModel(serviceRequest);
}

<h2 class="d-none d-md-block">Email Providers</h2>
<p>
    <a class="btn btn-primary" href="/@Model.Value.Name/admin/superadmin/emailproviders/create">
        Create
    </a>
</p>

<div class="table-responsive">
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Provider</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var emailProvider in viewModel.EmailProviders.OrderBy(x => x.Order))
            {
                <tr>
                    <td>
                        <a href="/@Model.Value.Name/admin/superadmin/emailproviders/@emailProvider.Id">@emailProvider.Name</a>
                    </td>
                    <td> 
                        <form method="post" action="/superadmin/groups/@Model.Value.Id/emails/providers/@emailProvider.Id/delete">
                            <button data-bs-toggle="tooltip" data-bs-title="Delete" class="btn-icon text-danger"
                                    onclick="return confirm('Are you sure you want to delete this email provider?');">
                                @await Html.PartialAsync("Components/_Icon", new IconViewModel(IconType.Times))
                            </button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>