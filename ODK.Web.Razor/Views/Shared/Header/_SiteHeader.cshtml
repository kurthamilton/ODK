@using ODK.Core.Platforms
@using ODK.Services.Platforms
@inject IPlatformProvider PlatformProvider
@{
    var platform = PlatformProvider.GetPlatform();
    if (platform != PlatformType.Default)
    {
        return;
    }
}

<nav class="navbar navbar-dark bg-dark text-light border-bottom border-light">
    <div class="container d-flex">
        <div class="me-3">
            <a href="/">SITE LOGO</a>
        </div>
        <div class="me-auto">
            @await Html.PartialAsync("Header/_SiteMenu")
        </div>

        <div>
            @await Html.PartialAsync("Header/_SiteAccountMenu")
        </div>
    </div>
</nav>