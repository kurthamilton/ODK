@using ODK.Core.Utils
@using ODK.Services.Chapters.ViewModels
@using ODK.Web.Common.Routes
@using ODK.Web.Razor.Models.Components
@using ODK.Web.Razor.Models.Contact
@model GroupConversationsPageViewModel
@inject IOdkRoutes OdkRoutes

@if (Model.Conversations.Count > 0)
{
    <h2>Conversations</h2>

    <div class="mt-3">
        @await Html.PartialAsync("Components/_Tabs", new TabsViewModel
        {
            MenuItems = new[]
            {
                new MenuItem { Active = !Model.Archived, Text = "Active", Link = OdkRoutes.Groups.Conversations(Model.Chapter) },
                new MenuItem { Active = Model.Archived, Text = "Archived", Link = OdkRoutes.Groups.Conversations(Model.Chapter, archived: true) }
            }
        })
    </div>

    @if (!Model.Archived)
    {
        <div class="mt-3">
            <button class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#new-conversation-modal">
                @await Html.PartialAsync("Components/_Icon", new IconViewModel(IconType.Message))
                <span>New conversation</span>
            </button>
        </div>
    }

    <section class="section mt-3">
        <div class="table-responsive">
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>Subject</th>
                        <th>Started</th>
                        <th>Last message</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var dto in Model.Conversations.OrderByDescending(x => x.LastMessage.Message.CreatedUtc))
                    {
                        <tr>
                            <td>
                                <a href="@OdkRoutes.Groups.Conversation(Model.Chapter, dto.Conversation.Id)">
                                    @dto.Conversation.Subject
                                </a>
                            </td>
                            <td>
                                @dto.Conversation.CreatedUtc.ToFriendlyDateTimeString(Model.TimeZone)
                            </td>
                            <td>
                                @dto.LastMessage.Message.CreatedUtc.ToFriendlyDateTimeString(Model.TimeZone)
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </section>  
    
    @await Html.PartialAsync("Components/_Modal", new ModalViewModel
    {
        Title = "Start new conversation",
        Id = "new-conversation-modal",
        Body = 
            @<div>
                <form action="/groups/@Model.Chapter.Id/conversations" method="post">
                    @*@Html.AntiForgeryToken()*@
                    @await Html.PartialAsync("Contact/_ConversationForm", new ConversationFormViewModel())
                    <button class="btn btn-primary">Send</button>
                </form>
            </div>
    })
}
else
{
    <h2>Start new conversation</h2>
    <section class="section mt-3">
        <form action="/groups/@Model.Chapter.Id/conversations" method="post">
            @*@Html.AntiForgeryToken()*@
            @await Html.PartialAsync("Contact/_ConversationForm", new ConversationFormViewModel())
            <button class="btn btn-primary">Send</button>
        </form>
    </section>
}