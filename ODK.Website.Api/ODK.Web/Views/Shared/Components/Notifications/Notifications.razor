<p>TODO - Notifications</p>
<div *ngIf="notifications.length" class="container">
    <div *ngFor="let notification of notifications; let i = index" class="mt-3">
        <app-notification [notification]="notification" 
                          [cssClass]="i === notifications.length - 1 ? 'mb-0' : ''"
                          (dismiss)="onDismiss(notification)">
        </app-notification>
    </div>
</div>

@code {
}
